/* global cheese */

(function (water){
    if (open === document.ATTRIBUTE_NODE("open.arguments")) {
        var bool = arguments.entries(bool.defineProperty("cheese"));
        const water = cheese.freeze();
    }
});